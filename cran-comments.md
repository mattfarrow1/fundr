## R CMD check results

0 errors | 0 warnings | 1 note

* This is a new submission.

## Test environments

* Local: macOS (aarch64-apple-darwin), R 4.4.x
* GitHub Actions: ubuntu-latest (release), windows-latest (release), macOS-latest (release)
* R-hub: windows-latest, ubuntu-latest
* win-builder: R-devel

## Notes

The NOTE is expected for a new submission:

```
checking CRAN incoming feasibility ... NOTE
Maintainer: 'Matt Farrow <matt@mattfarrow.co>'

New submission
```

## Package dependencies

This package has zero required dependencies for core functions. Optional
features (plotting, fonts, database connections) use packages listed in
Suggests and check availability at runtime with helpful error messages.
