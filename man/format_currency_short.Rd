% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format.R
\name{format_currency_short}
\alias{format_currency_short}
\title{Format currency in compact notation}
\usage{
format_currency_short(
  x,
  prefix = "$",
  digits = 1L,
  threshold = 1000,
  negative = c("minus", "parens")
)
}
\arguments{
\item{x}{Numeric vector to format.}

\item{prefix}{Currency symbol prefix. Default "$".}

\item{digits}{Number of decimal places for the shortened number. Default 1.}

\item{threshold}{Minimum absolute value to apply shortening. Values below
this are formatted as regular currency. Default 1000.}

\item{negative}{How to display negative numbers: "parens" or "minus".
Default "minus" (more common in compact formats).}
}
\value{
Character vector of formatted currency strings.
}
\description{
Formats large currency values using K (thousands), M (millions), or
B (billions) suffixes. Useful for chart labels and summary tables
where space is limited.
}
\examples{
format_currency_short(1500000)
#> "$1.5M"

format_currency_short(250000)
#> "$250K"

format_currency_short(500)
#> "$500"

format_currency_short(c(1000, 50000, 2500000, 1000000000))
#> "$1K" "$50K" "$2.5M" "$1B"

}
