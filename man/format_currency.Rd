% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format.R
\name{format_currency}
\alias{format_currency}
\title{Format numbers as currency}
\usage{
format_currency(
  x,
  prefix = "$",
  suffix = "",
  digits = 0L,
  big.mark = ",",
  decimal.mark = ".",
  negative = c("parens", "minus"),
  trim = TRUE
)
}
\arguments{
\item{x}{Numeric vector to format.}

\item{prefix}{Currency symbol prefix. Default "$".}

\item{suffix}{Currency symbol suffix. Default "".}

\item{digits}{Number of decimal places. Default 0 (whole dollars).}

\item{big.mark}{Thousands separator. Default ",".}

\item{decimal.mark}{Decimal separator. Default ".".}

\item{negative}{How to display negative numbers: "parens" for ($100),
"minus" for -$100. Default "parens".}

\item{trim}{Logical; if TRUE, trims leading whitespace. Default TRUE.}
}
\value{
Character vector of formatted currency strings.
}
\description{
Converts numeric values to formatted currency strings with proper
thousands separators and configurable decimal places. Handles NA values
and negative numbers gracefully.
}
\examples{
# Basic usage
format_currency(1234567)
#> "$1,234,567"

# With decimals
format_currency(1234.567, digits = 2)
#> "$1,234.57"

# Negative numbers
format_currency(-500)
#> "($500)"

format_currency(-500, negative = "minus")
#> "-$500"

# In a dplyr pipeline
# df |> mutate(gift_formatted = format_currency(gift_amount))

}
