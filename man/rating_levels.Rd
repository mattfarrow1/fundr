% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/buckets.R
\name{rating_levels}
\alias{rating_levels}
\title{Get rating level thresholds}
\usage{
rating_levels(
  preset = c("all", "small", "medium", "large"),
  include = NULL,
  bucket_map = NULL
)
}
\arguments{
\item{preset}{A preset name: "small", "medium", "large", or "all" (default).
\itemize{
\item "small": Major at $25K+, Principal at $250K+ (community foundations)
\item "medium": Major at $50K+, Principal at $1M+ (mid-size orgs)
\item "large": Major at $100K+, Principal at $5M+ (universities, hospitals)
\item "all": All available thresholds (comprehensive)
}}

\item{include}{Numeric vector of specific thresholds to include. If provided,
overrides \code{preset}. Values must exist in \code{fundr_rating_levels$rating_value}.}

\item{bucket_map}{Named character vector mapping thresholds to buckets.
Names should be threshold values (as character), values should be bucket
names ("Principal", "Major", "Mid-Level", "Annual", "Unrated").
Only used with \code{include}.}
}
\value{
A data frame with columns \code{rating_value}, \code{rating_level}, and
\code{rating_bucket}, suitable for use with \code{\link[=bucket_rating_level]{bucket_rating_level()}}.
}
\description{
Returns a subset of \code{fundr_rating_levels} based on a preset or custom
thresholds. Use this to get levels appropriate for your organization's
capacity rating scale.
}
\examples{
# Get small shop levels
rating_levels("small")

# Get large shop levels
rating_levels("large")

# Get all levels
rating_levels()

# Custom selection with custom bucket assignments
rating_levels(
  include = c(0, 1, 10000, 50000, 250000),
  bucket_map = c("0" = "Unrated", "1" = "Annual", "10000" = "Mid-Level",
                 "50000" = "Major", "250000" = "Principal")
)

# Use with bucket_rating_level()
ratings <- c(5000, 50000, 500000)
bucket_rating_level(ratings, levels = rating_levels("small"))

}
\seealso{
Other bucketing: 
\code{\link{bucket_gift_level}()},
\code{\link{bucket_rating_level}()},
\code{\link{gift_levels}()}
}
\concept{bucketing}
