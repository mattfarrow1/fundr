% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/buckets.R
\name{gift_levels}
\alias{gift_levels}
\title{Get gift level thresholds}
\usage{
gift_levels(preset = c("all", "small", "medium", "large"), include = NULL)
}
\arguments{
\item{preset}{A preset name: "small", "medium", "large", or "all" (default).
\itemize{
\item "small": Annual fund focus, thresholds from $1 to $100K
\item "medium": Leadership/major gifts, thresholds from $1K to $1M
\item "large": Principal gifts, thresholds from $100K to $150M+
\item "all": All available thresholds (comprehensive)
}}

\item{include}{Numeric vector of specific thresholds to include. If provided,
overrides \code{preset}. Values must exist in \code{fundr_gift_levels$ask_amount}.}
}
\value{
A data frame with columns \code{ask_amount}, \code{giving_level}, and \code{ask_bucket},
suitable for use with \code{\link[=bucket_gift_level]{bucket_gift_level()}}.
}
\description{
Returns a subset of \code{fundr_gift_levels} based on a preset or custom thresholds.
Use this to get levels appropriate for your organization's gift size distribution.
}
\examples{
# Get small shop levels (annual fund focus)
gift_levels("small")

# Get large shop levels (principal gifts)
gift_levels("large")

# Get all levels
gift_levels()

# Custom selection of thresholds
gift_levels(include = c(0, 0.01, 1000, 10000, 100000))

# Use with bucket_gift_level()
amounts <- c(500, 5000, 25000, 100000)
bucket_gift_level(amounts, levels = gift_levels("small"))

}
\seealso{
Other bucketing: 
\code{\link{bucket_gift_level}()},
\code{\link{bucket_rating_level}()},
\code{\link{rating_levels}()}
}
\concept{bucketing}
