% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/date_utils.R
\name{bucket_recency}
\alias{bucket_recency}
\title{Bucket dates by recency}
\usage{
bucket_recency(
  date,
  as_of = Sys.Date(),
  buckets = c(0, 1, 2, 5),
  labels = NULL,
  use_fiscal = FALSE,
  fy_start_month = 7L
)
}
\arguments{
\item{date}{Date vector (or coercible via \code{as.Date()}).}

\item{as_of}{Reference date for recency calculation. Default is today.}

\item{buckets}{Numeric vector of year boundaries for buckets.
Default \code{c(0, 1, 2, 5)} creates buckets: "This year", "Last year",
"2-5 years ago", "5+ years ago".}

\item{labels}{Character vector of bucket labels. Must be one longer than
\code{buckets} to include the final "X+ years ago" label. Default NULL
generates labels automatically.}

\item{use_fiscal}{Logical. If TRUE, uses fiscal year for bucketing.
If FALSE (default), uses calendar year.}

\item{fy_start_month}{Integer 1-12 for fiscal year start month.
Default 7 (July). Only used if \code{use_fiscal = TRUE}.}
}
\value{
Ordered factor of recency bucket labels.
}
\description{
Categorizes dates into recency buckets relative to a reference date.
Supports both calendar year and fiscal year bucketing.
}
\examples{
# Calendar year buckets
dates <- as.Date(c("2024-06-15", "2023-03-01", "2020-12-25", "2015-01-01"))
bucket_recency(dates, as_of = as.Date("2024-06-15"))

# Fiscal year buckets
bucket_recency(dates, as_of = as.Date("2024-06-15"), use_fiscal = TRUE)

# Custom buckets
bucket_recency(dates, as_of = as.Date("2024-06-15"),
               buckets = c(0, 1, 3, 10),
               labels = c("Current year", "Last year", "1-3 years",
                          "3-10 years", "10+ years"))

}
