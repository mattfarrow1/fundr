% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize_zip.R
\name{normalize_zip}
\alias{normalize_zip}
\title{Normalize postal (ZIP) codes}
\usage{
normalize_zip(
  postal,
  format = c("zip5", "zip9", "digits"),
  invalid = NA_character_,
  strict = TRUE,
  na_if_blank = TRUE
)
}
\arguments{
\item{postal}{Character vector of postal codes.}

\item{format}{Output format: "zip5" (always 5 digits), "zip9" (ZIP+4 when available),
or "digits" (raw digits, 5 or 9).}

\item{invalid}{Value to return for invalid postal codes.}

\item{strict}{Logical; if TRUE, only accept exactly 5 or 9 digits after cleaning.
If FALSE, will attempt to salvage by taking the first 5 digits when at least 5 are present,
and the first 9 digits when at least 9 are present.}

\item{na_if_blank}{Logical; if TRUE, blank/whitespace-only inputs become \code{invalid}.}
}
\value{
A character vector of normalized postal codes.
}
\description{
Normalizes US ZIP codes to either 5-digit ("12345") or ZIP+4 ("12345-6789")
format by removing punctuation and whitespace. Postal codes are treated as
character strings; leading zeros are preserved.
}
