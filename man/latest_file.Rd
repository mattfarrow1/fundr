% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/files.R
\name{latest_file}
\alias{latest_file}
\title{Find the most recent file in a folder}
\usage{
latest_file(
  path,
  pattern = NULL,
  full.names = TRUE,
  recursive = FALSE,
  by = c("mtime", "ctime", "atime")
)
}
\arguments{
\item{path}{Directory path to search in.}

\item{pattern}{Regular expression pattern to filter files. Default NULL
matches all files.}

\item{full.names}{Logical. If TRUE (default), returns the full file path.
If FALSE, returns just the filename.}

\item{recursive}{Logical. If TRUE, searches subdirectories recursively.
Default FALSE.}

\item{by}{How to determine "most recent": "mtime" (modification time,
default), "ctime" (creation/change time), or "atime" (access time).}
}
\value{
Character string with the path to the most recent file, or NULL
if no matching files are found.
}
\description{
Scans a directory for files matching a pattern and returns the path to
the most recently modified file. Useful for loading the latest data
export or report.
}
\examples{
# Create temporary files for demonstration
tmp <- tempdir()
writeLines("a", file.path(tmp, "file1.csv"))
Sys.sleep(0.1)
writeLines("b", file.path(tmp, "file2.csv"))

# Find the most recent CSV
latest_file(tmp, pattern = "\\\\.csv$")

# Clean up
unlink(file.path(tmp, c("file1.csv", "file2.csv")))

}
