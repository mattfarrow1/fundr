% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/buckets.R
\name{bucket_gift_level}
\alias{bucket_gift_level}
\title{Bucket gift ask amounts using fundr_gift_levels}
\usage{
bucket_gift_level(
  ask_amount,
  levels = fundr_gift_levels,
  value_col = "ask_amount",
  giving_level_col = "giving_level",
  bucket_col = "ask_bucket",
  what = c("giving_level", "ask_bucket"),
  na_value = NA
)
}
\arguments{
\item{ask_amount}{Numeric vector of ask amounts.}

\item{levels}{A levels table like \code{fundr_gift_levels}, or a preset name
("small", "medium", "large", "all"). See \code{\link[=gift_levels]{gift_levels()}} for details on
presets.}

\item{value_col}{Name of the numeric threshold column.}

\item{giving_level_col}{Name of the giving level label column.}

\item{bucket_col}{Name of the bucket label column.}

\item{what}{Which label to return: "giving_level" or "ask_bucket".}

\item{na_value}{Value to return when \code{ask_amount} is NA or cannot be bucketed.}
}
\value{
A factor (ordered if the source column is ordered), or character if source is character.
}
\description{
Maps numeric ask amounts to the nearest threshold in \code{fundr_gift_levels}.
Returns labels such as "$1,000,000+" and/or broader buckets.
}
\examples{
# Bucket gift amounts into giving levels
amounts <- c(500, 5000, 25000, 100000, 1500000, NA)
bucket_gift_level(amounts)

# Use a preset for smaller gift ranges
bucket_gift_level(amounts, levels = "small")

# Return broader ask buckets instead
bucket_gift_level(amounts, what = "ask_bucket")

# Vectorized for use in data frames
# df |> mutate(level = bucket_gift_level(gift_amount))

}
\seealso{
\code{\link[=gift_levels]{gift_levels()}} for creating custom level tables

Other bucketing: 
\code{\link{bucket_rating_level}()},
\code{\link{gift_levels}()},
\code{\link{rating_levels}()}
}
\concept{bucketing}
