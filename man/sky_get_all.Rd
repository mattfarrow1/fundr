% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sky_api.R
\name{sky_get_all}
\alias{sky_get_all}
\title{Get all pages from SKY API endpoint}
\usage{
sky_get_all(
  conn,
  endpoint,
  params = list(),
  limit = 500L,
  max_records = NULL,
  verbose = TRUE
)
}
\arguments{
\item{conn}{A SKY API connection created by \code{\link[=sky_connect]{sky_connect()}}.}

\item{endpoint}{API endpoint path.}

\item{params}{Named list of query parameters.}

\item{limit}{Records per page. Default 500 (max allowed).}

\item{max_records}{Maximum total records to retrieve. Default NULL (all).}

\item{verbose}{If TRUE, prints progress messages.}
}
\value{
Combined data frame of all records.
}
\description{
Handles pagination to retrieve all records from a SKY API endpoint.
}
\examples{
\dontrun{
conn <- sky_connect(
  subscription_key = Sys.getenv("SKY_API_KEY"),
  access_token = Sys.getenv("SKY_API_TOKEN")
)

# Get all constituents (handles pagination automatically)
all_constituents <- sky_get_all(conn, "constituent/v1/constituents")
}

}
