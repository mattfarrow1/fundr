% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database.R
\name{db_connect}
\alias{db_connect}
\title{Create database connection}
\usage{
db_connect(
  driver = c("sqlserver", "postgres", "mysql", "sqlite"),
  host = NULL,
  port = NULL,
  database,
  username = NULL,
  password = NULL,
  ...
)
}
\arguments{
\item{driver}{Database driver: "sqlserver", "postgres", "mysql", or "sqlite".}

\item{host}{Database server hostname or IP address.}

\item{port}{Database port (uses default if NULL).}

\item{database}{Database name.}

\item{username}{Database username.}

\item{password}{Database password. Consider using environment variables.}

\item{...}{Additional arguments passed to the DBI connection function.}
}
\value{
A DBI connection object.
}
\description{
A convenience wrapper for common database connections. Supports
SQL Server, PostgreSQL, MySQL, and SQLite.
}
\details{
Required packages by driver:
\itemize{
\item \strong{sqlserver}: odbc
\item \strong{postgres}: RPostgres
\item \strong{mysql}: RMySQL or RMariaDB
\item \strong{sqlite}: RSQLite
}

For security, store credentials in environment variables:
\preformatted{
# In .Renviron
DB_HOST=server.example.com
DB_USER=username
DB_PASS=password
}
}
\examples{
\dontrun{
# SQL Server connection
conn <- db_connect(
  driver = "sqlserver",
  host = Sys.getenv("DB_HOST"),
  database = "Advancement",
  username = Sys.getenv("DB_USER"),
  password = Sys.getenv("DB_PASS")
)

# Query and close
data <- DBI::dbGetQuery(conn, "SELECT * FROM Donors")
DBI::dbDisconnect(conn)

# SQLite (file-based, no credentials needed)
conn <- db_connect(driver = "sqlite", database = "local.db")
}

}
