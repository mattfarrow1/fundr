% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sky_api.R
\name{sky_get}
\alias{sky_get}
\title{Make GET request to SKY API}
\usage{
sky_get(conn, endpoint, params = list(), ...)
}
\arguments{
\item{conn}{A SKY API connection created by \code{\link[=sky_connect]{sky_connect()}}.}

\item{endpoint}{API endpoint path (e.g., "constituent/v1/constituents").}

\item{params}{Named list of query parameters.}

\item{...}{Additional arguments passed to httr.}
}
\value{
Parsed JSON response as a list or data frame.
}
\description{
Sends a GET request to the SKY API and returns the parsed response.
}
\examples{
\dontrun{
conn <- sky_connect(
  subscription_key = Sys.getenv("SKY_API_KEY"),
  access_token = Sys.getenv("SKY_API_TOKEN")
)

# Get constituents
result <- sky_get(conn, "constituent/v1/constituents",
                  params = list(limit = 100))
}

}
