% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scales.R
\name{scale_y_currency}
\alias{scale_y_currency}
\title{Currency scale for ggplot2 y-axis}
\usage{
scale_y_currency(
  prefix = "$",
  suffix = "",
  big.mark = ",",
  decimal.mark = ".",
  digits = 0L,
  short = FALSE,
  short_digits = 1L,
  negative = c("minus", "parens"),
  ...
)
}
\arguments{
\item{prefix}{Currency symbol prefix. Default "$".}

\item{suffix}{Currency symbol suffix. Default "".}

\item{big.mark}{Thousands separator. Default ",".}

\item{decimal.mark}{Decimal separator. Default ".".}

\item{digits}{Number of decimal places. Default 0.}

\item{short}{Logical; if TRUE, use compact notation (K/M/B). Default FALSE.}

\item{short_digits}{Decimal places for compact notation. Default 1.}

\item{negative}{How to display negative numbers: "parens" or "minus".
Default "minus".}

\item{...}{Additional arguments passed to \code{\link[ggplot2:scale_continuous]{ggplot2::scale_y_continuous()}}.}
}
\value{
A ggplot2 scale object.
}
\description{
Formats y-axis labels as currency values, avoiding scientific notation
and displaying proper dollar formatting. Supports both full format
($1,234,567) and compact format ($1.2M).
}
\examples{
\dontrun{
library(ggplot2)

# Sample data with large dollar amounts
df <- data.frame(
  category = c("A", "B", "C", "D"),
  amount = c(1500000, 2300000, 800000, 3100000)
)

# Default currency formatting
ggplot(df, aes(category, amount)) +
  geom_col() +
  scale_y_currency()

# Compact notation for large values
ggplot(df, aes(category, amount)) +
  geom_col() +
  scale_y_currency(short = TRUE)
}

}
