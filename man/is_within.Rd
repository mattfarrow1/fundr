% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/date_utils.R
\name{is_within}
\alias{is_within}
\title{Check if date is within a time period}
\usage{
is_within(
  date,
  within,
  unit = c("years", "months", "weeks", "days"),
  as_of = Sys.Date()
)
}
\arguments{
\item{date}{Date vector (or coercible via \code{as.Date()}).}

\item{within}{Numeric value for the time window.}

\item{unit}{Unit of time: "years", "months", "weeks", or "days".
Default "years".}

\item{as_of}{Reference date. Default is today.}
}
\value{
Logical vector. TRUE if date is within the specified period
of \code{as_of} (inclusive). NA for NA inputs.
}
\description{
Tests whether dates fall within a specified number of years, months,
or days from a reference date. Useful for filtering recent records.
}
\examples{
dates <- as.Date(c("2024-01-15", "2022-06-15", "2020-01-01"))

# Within last 2 years
is_within(dates, 2, "years", as_of = as.Date("2024-06-15"))
#> TRUE, TRUE, FALSE

# Within last 6 months
is_within(dates, 6, "months", as_of = as.Date("2024-06-15"))
#> TRUE, FALSE, FALSE

}
